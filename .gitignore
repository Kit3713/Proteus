# Rust
/target
**/*.rs.bk
*.pdb

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local
/.env
/.env.local

# Claude Code agent worktrees
/.claude/

# Integration test driver stages a freshly-built proteus here so the
# Containerfile's COPY can pick it up. Always rebuilt by run.sh; never
# checked in.
/tests/integration/proteus.bin
